// Storing the list of people to invite to dinner
const guestList: string[] = ["Farhan", "Maria", "Aslam"];

// Printing invitation messages to each person
for (const guest of guestList) {
  console.log(`Dear ${guest}, you are invited to a dinner at my place. It would be an honor to have you there.`);
}
