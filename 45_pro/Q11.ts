// Storing friends' names in an array
const names: string[] = ["Akber", "Roni", "Shahzad", "Zahid"];

// Printing each person's name using a loop
for (const name of names) {
  console.log(name);
}
