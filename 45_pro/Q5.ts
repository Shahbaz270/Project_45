const famousPerson1: string = "Albert Einstein";
const quote7: string = "A person who never made a mistake never tried anything new.";

const message7: string = `${famousPerson1} once said, "${quote7}"`;

console.log(message6);
